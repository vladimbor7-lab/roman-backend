# 🚀 Деплой Travel AI на Railway

## Что такое Railway
Railway — это хостинг для Node.js приложений. Бесплатный план даёт $5/месяц кредитов (хватает для старта). Ваш бэкенд будет доступен по URL типа `https://roman-backend-production.up.railway.app`

---

## Шаг 1 — Подготовка (5 минут)

### Установите Git (если нет)
- Windows: https://git-scm.com/download/win
- Mac: `brew install git`

### Зарегистрируйтесь на Railway
Зайдите на **railway.app** → Sign Up → войдите через GitHub

---

## Шаг 2 — Загрузите код на GitHub (3 минуты)

```bash
# 1. Создайте папку roman-backend и скопируйте туда все файлы
# (те что вы скачали из панели)

# 2. Откройте терминал в папке roman-backend

# 3. Инициализируйте Git
git init
git add .
git commit -m "Initial commit"

# 4. Создайте репозиторий на github.com (кнопка New Repository)
# Назовите: roman-backend
# Скопируйте ссылку вида: https://github.com/ВАШ_НИК/roman-backend.git

# 5. Загрузите
git remote add origin https://github.com/ВАШ_НИК/roman-backend.git
git push -u origin main
```

---

## Шаг 3 — Создайте проект на Railway (2 минуты)

1. Зайдите на **railway.app** → New Project
2. Выберите **Deploy from GitHub repo**
3. Выберите репозиторий `roman-backend`
4. Railway автоматически обнаружит Node.js и задеплоит

---

## Шаг 4 — Добавьте переменные окружения

В Railway → ваш проект → **Variables** → добавьте:

| Переменная | Значение |
|-----------|---------|
| `ANTHROPIC_KEY` | `sk-ant-api03-...` (ваш мастер-ключ) |
| `ADMIN_KEY` | придумайте надёжный секретный ключ |
| `ENCRYPTION_KEY` | ровно 32 символа, придумайте |
| `PORT` | `3000` |

---

## Шаг 5 — Получите URL

После деплоя Railway выдаст URL вида:
```
https://roman-backend-production.up.railway.app
```

**Замените в трёх файлах** строку:
```js
const API = 'http://localhost:3000';
```
на:
```js
const API = 'https://roman-backend-production.up.railway.app';
```

Файлы:
- `roman-login.html`
- `roman-agency-panel.html`
- `roman-tour-bot.html`

---

## Шаг 6 — Создайте первое агентство

```bash
curl -X POST https://ВАШ_URL/admin/agencies \
  -H "x-admin-key: ВАШ_ADMIN_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Первое агентство",
    "plan": "pro",
    "bot_name": "Роман"
  }'
```

В ответ получите `api_key` — выдайте его агентству для входа.

---

## Проверка что всё работает

```bash
# Health check
curl https://ВАШ_URL/health

# Ответ должен быть:
# {"status":"ok","version":"1.0.0","time":"..."}
```

---

## Полезные команды Railway CLI (опционально)

```bash
# Установка
npm install -g @railway/cli

# Войти
railway login

# Деплой из терминала (вместо GitHub)
railway up

# Посмотреть логи
railway logs
```

---

## После деплоя — полная схема работы

```
Клиент → roman-tour-bot.html
              ↓  /api/chat/message
         Railway сервер
              ↓  шифрованные credentials агентства
         Anthropic API + Sletat XML
              ↓  готовый ответ
         Клиент видит подборку туров
              ↓  нажимает "Передать менеджеру"
         Лид сохраняется в БД
              ↓
         Агентство видит в roman-agency-panel.html
```
